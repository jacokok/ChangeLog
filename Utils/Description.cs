namespace ChangeLog.Utils;

public static class Description
{
    public const string AllTypeDescription = @"Only compare specific types. 
        U = Table,
        V = View,
        P = Stored Procedure,
        FN = Function,
        IF = Inline Table Function,
        TF = Table Value Function,
        AF = Aggregate Function,
        FS = Scalar Function,
        CT = Custom Types";

    public const string GenerateTypeDescription = @"Only compare specific types. 
        U = Table,
        V = View,
        P = Stored Procedure,
        FN = Function,
        IF = Inline Table Function,
        TF = Table Value Function";
}